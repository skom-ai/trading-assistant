# =====================================================================
# File: .env.example
# Description: Environment template for the QuantMind docker-compose stack.
#              Copy to `.env` and adjust. Local defaults are dev-safe; do
#              NOT reuse these credentials outside local Docker.
# =====================================================================

# ---- PostgreSQL ----
POSTGRES_USER=quantmind
POSTGRES_PASSWORD=quantmind
POSTGRES_DB=quantmind

# ---- MongoDB (audit-only trace store) ----
MONGO_USER=root
MONGO_PASSWORD=root

# ---- Agent (FastAPI) ----
GEMINI_API_KEY=<GET API KEY>
# LLM provider: 'mock' (deterministic, offline default) or 'openrouter'.
QM_LLM_PROVIDER=gemini
QM_LLM_API_KEY=<GET API KEY>

# ---- Service-to-service auth (BFF -> Agent) ----
INTERNAL_API_TOKEN=dev-internal-token

# ---- BFF (NestJS) ----
CORS_ORIGINS=http://localhost:3000
RATE_LIMIT=60

# ---- UI (Vite) ----
VITE_BFF_BASE_URL=http://localhost:8080
